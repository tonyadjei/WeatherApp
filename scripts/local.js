// const todos = [
// 	{text: 'play mariokart', author: 'shaun'},
// 	{text: 'buy some milk', author: 'mario'},
// 	{text: 'buy some bread', author: 'luigi'}
// ];
// //turns it into a JSON string which can be stored in the browser's local storage(only stores strings)
// localStorage.setItem('todos', JSON.stringify(todos));
// //JSON.stringify() will convert an array of objects, into a JSON string format(properties will have double qoutes)

// const stored = localStorage.getItem('todos');
// console.log(JSON.parse(stored));
// //JSON.parse() will take a JSON string and parse it into its equivalent of an array of object